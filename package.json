{
  "name": "tts-extension",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm i && vite",
    "dev:firefox": "pnpm i && cross-env TARGET=firefox vite",
    "dev:edge": "pnpm i && cross-env TARGET=edge vite",
    "build": "pnpm i && tsc && vite build",
    "build:firefox": "pnpm i && tsc && cross-env TARGET=firefox vite build",
    "build:edge": "pnpm i && tsc && cross-env TARGET=edge vite build"
  },
  "dependencies": {
    "lodash.debounce": "^4.0.8",
    "rollup": "^4.28.0"
  },
  "devDependencies": {
    "@types/lodash.debounce": "^4.0.6",
    "@vicons/carbon": "^0.12.0",
    "@vicons/ionicons5": "^0.12.0",
    "@vicons/utils": "^0.1.4",
    "@vitejs/plugin-vue": "^5.2.1",
    "@vue/compiler-sfc": "^3.5.13",
    "cross-env": "^7.0.3",
    "naive-ui": "^2.40.3",
    "typescript": "^5.3.2",
    "vite": "^5.0.0",
    "vite-plugin-web-extension": "^4.3.1",
    "vue": "^3.3",
    "webextension-polyfill": "^0.12.0"
  }
}
